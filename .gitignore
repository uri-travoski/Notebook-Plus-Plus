# Secrets / local
docs/credentials.md
.claude/settings.local.json
CLAUDE.local.md
.env
.env.*
!.env.example

# Local docs & AI-assistant config (kept local, not published)
docs/
CLAUDE.md
global claude.md
DESIGN-NOTES.md
AGENTS.md

# Dependencies / build output
node_modules/
.nuxt/
.output/
.data/
dist/
*.log
npm-debug.log*

# Build coordination (ephemeral)
.notebookpp-build/
.impeccable/

# Test artifacts
e2e/artifacts/
test-results/
playwright-report/
coverage/

# Self-hosted Excalidraw fonts (copied from node_modules on postinstall)
public/fonts/

# Bundled starter-content template (baked into the private image, not committed to git). The
# dir itself is kept via .gitkeep so a source build ships an empty template.
server/db/seed/template.json
server/db/seed/uploads/

# OS
.DS_Store
Thumbs.db
