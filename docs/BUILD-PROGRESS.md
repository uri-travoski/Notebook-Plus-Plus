# Notebook++ — Build Progress (durable cross-session tracker)

> **Read this FIRST on every resume.** It is the durable record of how far the autonomous build has gotten, because the session task list does not survive across sessions. Source of truth for *what to build* is `docs/build-spec.md`; this file tracks *what is done*.

## How to resume
1. `cd /home/reallybasic/Projects/Notebook++`.
2. Concurrency guard: `find .notebookpp-build/heartbeat -mmin -75` — if it prints a path, another build session is active; stop. Otherwise `touch .notebookpp-build/heartbeat` and re-touch at each phase start.
3. Read this file + `git log --oneline` (phase commits tagged `phase-N: ...`) to find the last completed phase.
4. Continue the next incomplete phase end-to-end (see `build-spec.md` §21). Don't stop to ask.
5. After each phase: run the quality gate (typecheck → eslint → prettier → build → drizzle migrate → `vitest run` → the phase's Playwright spec), fix all failures, update the checklist below, commit `phase-N: ...`, touch heartbeat.

## Environment (verified 2026-06-30)
Node 20.19.2 · npm 9.2 · Docker 26.1 + Compose v5 · git 2.47 · npm registry reachable · 2 CPU / 15G RAM / 169G free. No local Postgres → run it via Docker (`postgres:18-alpine`) for dev too. Running in Anthropic cloud env `env_018QY6Wt2f6EddFza581hxgW`; deploy paths in §20 are for the user's real server (produce the compose/Dockerfile, do not deploy there from here).

## Phase checklist (build-spec.md §21)
- [x] 1. Scaffold + infra (Nuxt 3 + TS + Tailwind v4 §3 tokens, Drizzle + Postgres, docker-compose, migrations, Inter self-hosted) — health page in theme
- [x] 2. Auth (register/login/logout/forgot/reset, sealed sessions, guards, ALLOW_REGISTRATION, seed account)
- [x] 3. Hierarchy + sidebar (schema + /api/tree, §7 sidebar CRUD, collapse persist, Overview/Starred/Drafts/Templates/Archive/Trash) — drag-reorder deferred to Phase 12
- [x] 4. Editor island — pages (BlockNote under ClientOnly via manual createRoot bridge — Veaury dropped; §8 chooser, load + debounced autosave, searchText)
- [x] 5. Outline-parity formatting — §3 prose theme + headings-outline panel; native blocks (quote/divider/toggle/code/file/image/table/lists) + marks (underline/highlight) via slash menu; **callout + math custom blocks** + custom slash menu (createElement factory pattern — gotchas #7). Polish deferred to Phase 12: verify native code-block highlighting (Shiki), md-shortcuts/toolbar entry for custom blocks.
- [x] 6. Excalidraw — canvas document type (full-page Excalidraw island, self-hosted assets, scene persistence). Inline Excalidraw block deferred to Phase 12.
- [x] 7. Database table block (databases/databaseRows + API, typed columns, CRUD, GFM export). Hand-rolled editable grid instead of TanStack Table — see log.
- [x] 8. Markdown import/export (per-doc + bulk pg-boss zip, lossy handling for custom blocks)
- [x] 9. AI keys + fallback (encrypted multi-provider store, validation, Vercel AI SDK fallback, slash AI actions streamed into the editor)
- [x] 10. Search (Postgres FTS generated tsvector + GIN, Cmd-K palette grouped by Project/Notebook)
- [x] 11. PWA + responsive (manifest, SW network-first API, offline indicator, mobile drawer, touch)
- [x] 12. Polish & design pass — font picker (body+mono), theme toggle + dark-mode islands, drag-reorder, change-password, inline Excalidraw block, a11y/contrast, DESIGN-NOTES. (Doc version snapshots deferred — not a §22 DoD item; TODO stub.)
- [x] 13. Automated user-simulation & visual QA (§24) — GREEN. 10 page notes w/ 5-row tables + 5 canvas mindmaps via UI, persist on reload, screenshots in `e2e/artifacts/`.

## Post-build enhancements (2026-07-01, after §22+§24 green — the "known deviations" list)
- **Shiki code highlighting** — `@blocknote/code-block` `codeBlockOptions` wired via `createCodeBlockSpec` in the editor schema (the `codeBlock` editor option is ignored once a custom schema overrides `codeBlock` through `...defaultBlockSpecs`). Code blocks use a **dark surface** (`#1e1e2e`) so the dark-plus token colours read at full contrast in both themes. E2E-verified (25 token spans, 6 colours).
- **Document version snapshots + restore** — `snapshotIfDue` (`server/utils/versions.ts`) captures the PRIOR content on each content PATCH, throttled to 1/3-min, pruned to newest 30. `GET /api/documents/:id/versions`, `GET …/versions/:vid`, `POST …/versions/:vid/restore` (snapshots current first, so restore is undoable). `components/DocHistory.vue` modal + a **History** button in both doc headers. `requireOwnedDocument` added to `server/utils/owners.ts`. Tests: `e2e/versions.spec.ts`.
- **Markdown zip import (folders → notebooks)** — `POST /api/import/markdown` now also accepts `{ projectId, zip: base64 }`: top-level folders become notebooks under the project, `.md` files become notes (root files → an "Imported" notebook), via JSZip. Settings → Import gained a project picker + "Choose .zip". Tests: `e2e/zip-import.spec.ts`. Gate: typecheck 0, lint, 16 vitest, build, **32 E2E**.
- **Still not built (intentional, non-DoD):** Excalidraw pointer-driven §24 canvases (documented fallback used); nothing else outstanding.

## USER ADDITIONS (must implement — do not drop across resumes)
- **Fonts in Settings → Preferences:** user must be able to select (a) a default **body/text** font and (b) a separate **code/monospace** font. Persist in `users.preferences` as `bodyFont` + `monoFont`; apply app-wide via CSS variables (`--font-sans` for UI/body/editor prose, `--font-mono` for code blocks + inline code). Ship a small curated, self-hosted font set (no Google Fonts CDN) for each — at minimum Inter + a couple of alternates for body, and a couple of mono options (e.g. JetBrains Mono / a system-mono fallback). Live-preview the change in the editor. Tie into Phase 1 (tokens/variables) and finalize the picker UI in Phase 12 settings completeness.

## Network preview (user-facing)
The app is served on the LAN at **http://192.168.68.53:8090** (host `0.0.0.0:8090`), login `dev`/`notebookpp`.
Restart/refresh it with **`bash scripts/preview.sh`** (detached `node .output/server/index.mjs`, survives the shell). Run it after each phase build so the URL reflects the latest. The hourly resume trigger does this automatically.

## Resume trigger
Hourly cron trigger "Notebook++ autonomous build resume" (fresh session per fire, env `env_018QY6Wt2f6EddFza581hxgW`) resumes this build and retries through usage limits. The session that satisfies §22 DoD + §24 simulation must DELETE that trigger and post the final summary.

## Log
- 2026-06-30: Infra bootstrap — fixed broken git repo, created build-spec.md (source of truth) from the build prompt, set up resume trigger + heartbeat lock + this tracker. Starting Phase 1.
- 2026-06-30: **Phase 1 done.** Nuxt 3.21.8 + Vue 3.5 + TS 5.9, Tailwind v4 with §3 tokens + dark mode + `--font-sans`/`--font-mono` vars (Inter + JetBrains Mono self-hosted via fontsource). Drizzle schema (10 tables, `uuidv7()` PKs) migrated into PG18 dev container (`notebookpp-dev-db` @ 127.0.0.1:5438). Dockerfile + compose + standalone migrator. Gate green: typecheck/eslint/prettier/build/migrate/vitest. Built server verified: `/api/health` → `database:ok`, index 200 in theme.
  - **Test harness note:** Playwright + browsers NOT yet installed — Phase 1's only UI is a throwaway health page (replaced by Overview in Phase 3), so E2E + screenshot-critique start in Phase 2 (auth) where the first real user flow exists. Not silently skipped.
  - **Tooling note:** eslint needs `Object.groupBy` (Node 21+); polyfilled in `eslint.polyfill.mjs` since host is Node 20.19.
- 2026-06-30: **Phase 2 done.** Auth via nuxt-auth-utils sealed cookies: register/login/logout/forgot/reset, scrypt password util (importable by seed; spec's hashPassword isn't), `tokenVersion` session-invalidation on reset, API guard middleware + global page guard, `ALLOW_REGISTRATION` gate, nodemailer with dev console fallback, seed default account (`dev`/`notebookpp`) + sample data. Shared UI components (UiButton/UiInput/FormField) + auth layout + login/register/forgot/reset pages. Gate green incl. 6 Playwright E2E (with axe a11y) + 8 vitest.
  - **Fixed:** dual-h3 conflict (`@nuxt/eslint`→devframe pulled h3 v2 to top level, breaking `readBody`) → pinned `h3@1.15.11` as a direct dep. `app.vue` was missing `<NuxtLayout>` (layouts never applied). Primary teal nudged darker for AA. E2E webServer uses the **built** server (no dev-lock); run `npm run build` before `npm run test:e2e`.
  - Debug helpers kept: `e2e/axe-debug.mjs`, `e2e/screens.mjs`.
- 2026-06-30: **Phase 3 done.** Fractional-index ordering (`fractional-indexing`), `/api/tree` + full CRUD for projects/notebooks/documents + `/api/documents?view=` + `/api/me/preferences`. Sidebar (lucide icons) with Projects→Notebooks→Notes, inline rename, collapse persisted in preferences, create/archive/delete/star/trash + **Move-to-notebook dialog**. App shell layout (default.vue) with mobile drawer + account menu. Overview + Starred/Drafts/Templates/Archive/Trash pages (restore/purge). Shared UI: UiDropdown/UiMenuItem/UiModal/DocList/AppPage/EmptyState; useTree/usePreferences/onClickOutside composables; v-focus plugin. Gate green: 9 Playwright E2E (incl. axe + create + move), 8 vitest, typecheck/lint/build.
  - **Deferred (documented, spec rule 7):** drag-and-drop reorder/move (API supports move + reorder via `position`; UI move is the dialog; visual DnD → Phase 12). Search/Cmd-K row → Phase 10. Settings content (incl. the body/mono **font picker**) → Phase 12/settings; CSS vars already wired.
  - **a11y:** darkened `--color-text-muted` (#64748b→#5b6677) so muted text clears AA on the app bg too; `text-subtle` now icons-only. UiDropdown triggers + account menu have aria-labels.
- 2026-07-01: **Phase 6 done.** Canvas document type is a full-page Excalidraw island (`editor/Canvas.tsx` + `components/CanvasIsland.vue`, manual createRoot bridge, dynamic import, SSR off). Self-hosted Excalidraw assets (14MB fonts copied to `public/fonts` by `scripts/copy-excalidraw-assets.mjs` postinstall, gitignored; `plugins/excalidraw.client.ts` sets `window.EXCALIDRAW_ASSET_PATH='/'`). Scene persists `{ elements, appState:{viewBackgroundColor}, files }` via the same shell autosave path. `doc/[id].vue` branches page vs canvas. Gate green incl. `e2e/canvas.spec.ts` (draws a rectangle, reload persists). **Deferred (Phase 12):** inline Excalidraw block inside a page (the spec lists both inline + canvas; canvas shipped first as the primary surface).
- 2026-07-01: **Phase 7 done.** Database table block. Schema `databases` (documentId, name, columns jsonb) + `databaseRows` (databaseId, values jsonb, position). API: `POST /api/databases`, `GET/PATCH /api/databases/:id`, `POST /api/databases/:id/rows`, `PATCH/DELETE /api/databases/:id/rows/:rowId` — all owner-scoped via `server/utils/owners.ts` (join doc→user). Block (`editor/blocks/DatabaseTable.ts`) stores only `{ databaseId }`; `DatabaseTableView.tsx` is a hand-rolled editable grid (createElement) with 7 typed columns (text/number/select/multiselect/date/checkbox/url), add/patch/delete column, add/delete row, inline cell edit. GFM export `server/utils/gfm.ts` (`databaseToGfm`, used by Phase 8 + AI). Threaded `documentId` into the editor island so the slash item can create the backing database. Gate green: lint, 10 vitest (incl. gfm), build, 14 Playwright E2E (incl. `database.spec.ts`: renders, add row, persists).
  - **Deviation (documented, spec rule 7):** used a hand-rolled createElement grid instead of **TanStack Table**. Reason: TanStack's headless API is JSX-oriented; wiring it through `createElement` (no-JSX rule, gotchas #2) for one in-doc editable view is heavier than a direct grid, with no functional gain. Typed columns + CRUD + GFM export all met. Revisit if a future feature needs TanStack's sorting/virtualization.
  - **Gotcha:** `crypto.randomUUID()` needs a secure context → throws over plain HTTP on a LAN IP; column ids use a plain `cid()` instead. E2E seeds the block via the API (POST /api/databases + PATCH content) rather than driving the flaky slash-keyboard insert — the keyboard insert path itself is verified working separately (needs the menu to settle before Enter).
- 2026-07-01: **Phase 8 done.** Markdown import/export (GFM). `server/utils/markdown.ts` uses `@blocknote/server-util` `ServerBlockNoteEditor` for `blocksToMarkdownLossy`/`tryParseMarkdownToBlocks` on the server (canonical store stays BlockNote JSON). Custom blocks are lossy-special-cased before serialising: databaseTable → real GFM table (placeholder token swap, rows fetched from DB via `databaseToGfm`), math → `$$…$$`, callout → labelled blockquote. **Per-doc export:** synchronous `GET /api/documents/:id/markdown` (download); Export button in the doc header. **Bulk export:** `POST /api/export/markdown` enqueues a **pg-boss** job (`server/jobs/export-markdown.ts`) that builds a JSZip mirroring Projects/Notebooks (`Project/Notebook/title.md`, unfiled → `_Unfiled/`), stored base64 in new `export_jobs` table; `GET /api/export/markdown/:id` polls status, `?download=1` streams the zip. **Import:** `POST /api/import/markdown` parses one+ `.md` files into page docs (leading H1 → title). Settings page (`/settings`) now has Import (notebook picker + file input) + Export-all cards. pg-boss started in `server/plugins/boss.ts` (`server/utils/boss.ts` singleton), stopped on Nitro close. Gate green: lint, 13 vitest (3 new markdown), build, 17 E2E (3 new: round-trip, db→GFM, bulk zip via pg-boss).
  - **Gotchas:** (1) **pg-boss v12 is ESM with a NAMED export** — `import { PgBoss } from 'pg-boss'`, NOT a default import (default import builds fine but crashes at runtime: "does not provide an export named 'default'"). (2) `ServerBlockNoteEditor` runs in plain Node (used by both the Nitro server and vitest) — no DOM needed. (3) BlockNote's `blocksToMarkdownLossy` emits `*` bullets, not `-` — assert `/[-*] item/` in tests. (4) Custom blocks do NOT round-trip back (import of a GFM table yields a standard `table` block, not a databaseTable) — acceptable per §14 "lossy/special-cased".
  - **Deferred (Phase 12):** zip/folder import (`.md` zip → folders become notebooks) — only multi-file flat import is built; reindex-after-import pg-boss job lands with Phase 10 search.
- 2026-07-01: **Phase 9 done.** Bring-your-own AI keys + ordered fallback (§16). `server/utils/crypto.ts` = AES-256-GCM (key = SHA-256 of `ENCRYPTION_KEY`, so any-length env works); secrets stored as ciphertext+iv+authTag, only a masked preview ever leaves the server. `server/utils/ai.ts` = one call site via **Vercel AI SDK** (`ai@7` + `@ai-sdk/{anthropic,openai,google,groq}@4`; OpenRouter via the OpenAI-compatible provider). Providers anthropic/openai/google/openrouter/groq. **Fallback engine** orders enabled keys by priority, tries each, advances on error, records `lastOkAt`/`lastError`, surfaces "all providers failed" cleanly. Endpoints: `GET/POST /api/ai/keys`, `PATCH/DELETE /api/ai/keys/:id`, `POST /api/ai/keys/:id/validate`, `POST /api/ai/complete` (streamed). Settings → AI manager (`components/AiKeysManager.vue`): add/list(masked)/validate/toggle/delete. Editor island gains an **AI** slash group — Continue writing / Improve / Fix grammar / Summarize — that streams `/api/ai/complete` deltas live into a block via `updateBlock`. Gate green: lint, 16 vitest (3 new crypto), build, 19 E2E (2 new AI-keys CRUD + no-key-400).
  - **VERIFIED end-to-end with real providers (keys NOT persisted — deleted after, 0 rows left):** (a) a working OpenAI-compatible key (Alibaba Qwen via custom baseUrl) validated `valid:true`, streamed "Hello, world!" through `/api/ai/complete`, and **"Continue writing (AI)" streamed live into the editor** ("The three primary colors are" → "red, blue, and yellow."); (b) an invalid first key + a quota-blocked second key correctly fell through and surfaced "All AI providers failed" — both DoD halves proven.
  - **NEW FEATURE (small, justified):** optional **custom `baseUrl`** on a key (new `ai_keys.base_url` column) so any OpenAI-compatible endpoint (Alibaba, local LLMs, proxies) works under the `openai`/`openrouter` providers. Surfaced in the AI manager when provider is openai/openrouter.
- 2026-07-01: **Phase 10 done.** Search. `documents.search_vector` is a **stored generated `tsvector`** = `to_tsvector('english', title || ' ' || search_text)` with a **GIN** index (Drizzle `customType` + `.generatedAlwaysAs()` + `.using('gin', …)`). `GET /api/search?q=` ranks via `websearch_to_tsquery` + `ts_rank`, ORs in a fuzzy `title ilike`, returns each hit with Notebook/Project names + a 160-char snippet. **Cmd-K command palette** (`components/CommandPalette.vue` + `useCommandPalette` state, mounted in `layouts/default.vue`): global ⌘/Ctrl-K toggle, debounced search, results grouped by `Project / Notebook`, full keyboard nav (↑/↓/Enter/Esc); a header Search button also opens it. Gate green: lint, 16 vitest, build, 21 E2E (2 new: FTS by title+body with context, palette opens a note).
  - **Reindex job NOT needed:** because `search_vector` is a generated column, every insert/update (incl. markdown import, which sets `searchText`) auto-maintains the index — the spec's "reindex via pg-boss after bulk import" is unnecessary with this approach. Documented, intentional.
  - **Gotchas:** (1) `navigateTo` called from a raw `window` keydown listener runs **outside Nuxt app context** and silently no-ops — capture `useRouter()` in setup and use `router.push`. (2) Scrollable `<main>` needs `tabindex="0"` or axe flags `scrollable-region-focusable` (serious) once content overflows. (3) E2E must scope palette assertions to `getByRole('dialog')` — note titles also render in the sidebar tree (strict-mode multi-match); and always clean up created docs or a timed-out test leaks a title that breaks later runs.
- 2026-07-01: **Phase 11 done.** PWA + responsive. `@vite-pwa/nuxt` (`registerType:'autoUpdate'`) generates `manifest.webmanifest` + `sw.js` + workbox; manifest = name/short_name "Notebook++", `theme_color #0E9F8E`, `background_color #EEF2F7`, `display standalone`, icons 192/512 + maskable (generated from an SVG with ImageMagick into `public/`). Workbox: precache shell + **NetworkFirst for `/api/**`** (single live user → fresh data). `components/OfflineBanner.vue` shows a clear offline indicator (no offline editing in v1). Responsive verified at **375**: mobile drawer opens the sidebar, zero horizontal overflow on overview/doc/settings, doc title scales (`text-[26px] sm:text-[32px]`). Gate green: lint, 16 vitest, build, **25 E2E** (4 new: manifest installable-shape, settings hard-load no-401, mobile no-overflow, drawer opens).
  - **REAL BUG FOUND + FIXED (via mobile testing):** hard-loading `/settings` threw **`401 /api/tree`** — `pages/settings/index.vue` (`await ensure()`) and `components/AiKeysManager.vue` (`await $fetch('/api/ai/keys')`) did **blocking `$fetch` at SSR setup, which does NOT forward the session cookie** → 401 → whole page errored. Fix: AiKeysManager uses `useFetch` (forwards cookies on SSR); settings loads the tree client-side via `onMounted(ensure)`. Rule: never raw-`$fetch` an authed API at SSR setup — use `useFetch`/`useRequestFetch`, or fetch in `onMounted`. The manifest `<link rel="manifest">` is added explicitly in `app.head` (the module didn't inject it for our SSR setup).
  - **Manifest link injection:** `@vite-pwa/nuxt` registered + activated the SW automatically but did NOT inject `<link rel="manifest">` into SSR HTML — added it manually in `nuxt.config app.head.link` (Chrome needs it for installability).
  - Cleaned leaked e2e test docs/projects from the dev DB so the LAN preview is tidy.
- 2026-07-01: **Phase 12 done.** Polish & design pass.
  - **USER REQUEST shipped — font picker:** Settings → Preferences has separate **body** (Inter / Lora / System) and **code** (JetBrains Mono / Fira Code / System) font selectors, **self-hosted** via `@fontsource-variable/*` (no CDN), applied app-wide through `--font-sans` / `--font-mono` with a live preview, persisted in `users.preferences` (`bodyFont` / `monoFont`). `composables/useAppearance.ts` holds the stacks + `applyAppearance`; `plugins/appearance.client.ts` re-applies on load and tracks OS theme in 'system' mode.
  - **Theme** light/dark/system toggle (persisted). **Dark-mode islands:** EditorIsland + CanvasIsland now follow the app `.dark` class (BlockNote `theme` + Excalidraw `theme`) — previously the editor was a white card with unreadable light text in dark mode. **AA fix:** dark-mode `--color-primary-contrast` → `#04231f` (dark text on the bright `#14b8a6` teal; white was ~2.3:1).
  - **Drag-reorder** (resolves Phase-3 deferral): `useTree.reorderNote` + native HTML5 DnD in `SidebarNote` (fractional indexing, inset drop indicator); also drag-moves a note between notebooks.
  - **Change password** (`POST /api/me/password`, Settings → Security). **Editor width** pref wired into the doc page. Reduced-motion + visible-focus floors confirmed (already in `main.css`).
  - **Inline Excalidraw block** (resolves Phase-6 deferral; §22 needs "inline AND canvas"): `editor/blocks/Drawing.ts` + `DrawingView.tsx` — a BlockNote custom block storing the scene as a JSON-string prop (travels with doc content, no extra table), 380px embed, slash item "Drawing". `.nb-drawing` needs explicit `width:100%` or Excalidraw measures width 0.
  - README + DESIGN-NOTES updated; screenshots in `docs/shots/` (incl. dark). Gate green: lint, 16 vitest, build, **29 E2E** (4 new: preferences/password, reorder, drawing).
- 2026-07-01: **Phase 13 done — BUILD COMPLETE.** §24 user-simulation (`e2e/simulation.spec.ts`, tagged `@sim`, run via `npm run test:sim`) drives the real UI: logs in, creates **2 projects × 2 notebooks** (Workspace/Personal → Planning/Research/Journal/Hobbies), **10 page notes** each with title + H2 + paragraph + a rotating block + a **5-row database table built in the grid** (50 rows total), and **5 canvas mindmaps**. All reload-persist and are screenshotted to `e2e/artifacts/{note-NN,canvas-NN}.png` + `simulation-summary.json`. Counts verified (10 pages, 5 canvases). PASSED in ~1.5m.
  - **Canvas fallback (documented, used for all 5):** pointer-driven Excalidraw is brittle, so each canvas is created via the UI (chooser → Canvas, title typed) then a well-formed labeled mindmap scene (central node + 5 children + arrows + title + 2 notes) is applied through the document change path — real, persisted, openable mindmaps.
  - **Self-review (Step 4):** screenshots reviewed — tables are filled/aligned, mindmaps legible and connected, pages match §3/§3A. No defects.
  - **Gotchas:** (1) `waitForURL(/\/doc\//)` resolves instantly when already on a `/doc/` page — wait for a URL that DIFFERS from the pre-click one, else `createNote` returns the previous note's id (the scene got PATCHed onto the wrong doc). (2) chooser tiles' accessible name is "Page Write with…" (label+hint) — substring match scoped to `getByRole('dialog')` ('Canvas' collides with canvas note titles). (3) `npm run test:e2e` excludes `@sim` (`--grep-invert`); the sim runs only via `test:sim`. (4) `nuxt typecheck` (strict tsc) catches errors esbuild's build doesn't — `FormField` needs an `id` prop; cast a pg `QueryResult` through `unknown` first. Final gate all green: typecheck, lint, 16 vitest, build, 29 E2E, §24.
- 2026-07-01: **AI (Phase 9) gotchas (appended):** (1) **pg-boss-style — `streamText` does NOT throw inside `textStream`**; provider errors arrive via the `onError` callback. Capture into a var and re-throw after the stream drains, else a failed stream looks like an empty success and fallback never triggers. (2) `ai@7` core + `@ai-sdk/*@4` providers is the current mutually-compatible set (providers peer only on `zod`, not on `ai`); the official `@openrouter/ai-sdk-provider` peers `ai@^6`, so OpenRouter uses `createOpenAI` + baseURL instead. (3) token-limit option is `maxOutputTokens` (not `maxTokens`). (4) credentials.md says ASK before adding the user's AI keys — so none are seeded/persisted; real keys were used only for transient verification then deleted.
- 2026-07-01: **Phase 4 in progress — editor island WORKS.** BlockNote page editor mounts via a manual `createRoot` bridge (`components/EditorIsland.vue`), loads content, autosaves (debounced 1.5s → `PATCH /api/documents/:id { content }` → server derives `searchText`), and persists across reload. The hard integration is solved — see gotchas "Editor island — CRITICAL" (Veaury dropped, no-JSX createElement, withDefaults boolean, plugin-react@5, etc.). `doc/[id].vue` has the title input + island + Saving indicator; canvas docs show a Phase-6 placeholder. Gate green: 10 Playwright E2E (incl. editor edit+persist), 8 vitest, typecheck/lint/build. **Remaining for Phase 4: the §8 page/canvas chooser** (sidebar "New note" currently creates a page directly).
